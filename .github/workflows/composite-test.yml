name: Composite
on: 
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Run custom Java build action
        uses: ./.github/actions/composite-action
        with:
          java-version: 17
