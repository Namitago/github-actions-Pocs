name: Composite
on: 
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Run Composite Action for Java
        uses: ./.github/actions/composite-action
        with:
          java-version: "21"
          run-tests: "true"
